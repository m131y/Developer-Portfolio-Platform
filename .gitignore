#################################################################
# 1. IDE 설정 및 OS 파일 (필수)
#################################################################

# JetBrains IDEs (IntelliJ, WebStorm, PyCharm 등)
# 어느 폴더에 생성되든 모든 .idea 폴더를 무시합니다.
.idea/

# 이 외의 IDE 설정 파일
.vscode/           # VS Code 설정
.DS_Store          # macOS 임시 파일
*~
Thumbs.db          # Windows 임시 파일

#################################################################
# 2. 빌드 결과물 (Build Artifacts)
#################################################################

# --- [ Backend: Java/Spring Boot (Maven/Gradle) ] ---
# 컴파일된 클래스 파일
*.class
*.jar
*.war
*.ear
*.zip
*.tar
*.log
!README.md

# Maven 빌드 폴더 (일반적으로 사용)
/target/

# Gradle 빌드 폴더 (일반적으로 사용)
/build/
.gradle/

# IntelliJ IDEA에서 사용하는 별도의 빌드 폴더
/out/

#################################################################
# 3. Node.js 및 프론트엔드 (Frontend)
#################################################################

# 모듈 및 의존성
/node_modules

# 프론트엔드 빌드 출력 폴더 (React, Vue, Angular 등)
# 경고: 폴더 이름이 다르면 추가해야 합니다.
/dist
/build
/out

# 캐시 및 임시 폴더
/tmp
npm-debug.log
yarn-error.log
package-lock.json      # npm을 사용하지 않고 yarn만 사용한다면 무시 가능
.npm/
.nuxt/                 # Nuxt.js 캐시
.angular/              # Angular 캐시

#################################################################
# 4. 로컬 환경 설정 및 보안 관련 파일
#################################################################

# 로컬 환경 변수 파일 (API 키, DB 접속 정보 등 민감한 정보 포함)
.env
.env.local
.env.*.local
.env.development
.env.production

# Spring Boot의 로컬 설정 파일 (팀원별 DB 정보가 다를 때)
application-local.properties
application-local.yml

# 기타 보안/개인 파일
*.key
*.pem
*.p12

#################################################################
# 5. 테스트 및 Coverage 결과
#################################################################

/coverage
/lcov-report
